# BulletMaster

A side scrolling game engine written in Java.
This was a hobby project from 2013-2014.

## Build & Run

```bash
make      # compile
make run  # compile (if needed) and run
make clean
```

The game must be launched from the project root so that the `levels/` directory is on the relative path.
